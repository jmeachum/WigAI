# Story 2.3 Test Evidence (2026-02-16)

## Scope

- Story: `2-3-standard-track-targeting-contract-index-exact-name-selected-default`
- Focus: standard track-targeting contract (`track_index` -> `track_name` -> selected fallback), dual-selector confirmation semantics, ambiguity safety regression, docs/runtime/test lockstep.

## Executed Commands

1. Targeted contract/tool/facade/controller regression suite:

```bash
./gradlew test --tests "*TrackTargetingContractTest" --tests "*GetTrackDetailsToolTest" --tests "*ListDevicesOnTrackToolTest" --tests "*GetDeviceDetailsToolTest" --tests "*ClipToolTest" --tests "*ClipSceneControllerTest" --tests "*BitwigApiFacadeTest" --tests "*ErrorContractComplianceTest"
```

- Result: `BUILD SUCCESSFUL`

2. Full regression suite:

```bash
./gradlew test
```

- Result: `BUILD SUCCESSFUL`

## Notes

- Full test report generated by Gradle at:
  - `build/reports/tests/test/index.html`
